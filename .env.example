# ===========================================
# 冷凍乾燥設備預約系統 - 環境變數範本
# Cryo Equipment Booking System - Environment Variables Template
# ===========================================

# 🗃️ 資料庫設定 (Database Configuration)
# PostgreSQL 連接字串 - 請替換為您的實際資料庫資訊
DATABASE_URL="postgresql://username:password@localhost:5432/cryo_booking?schema=public"

# Vercel Postgres (如果使用 Vercel Postgres)
POSTGRES_URL="postgresql://username:password@host:port/database"
POSTGRES_PRISMA_URL="postgresql://username:password@host:port/database?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgresql://username:password@host:port/database"

# 🔐 NextAuth.js 認證設定 (Authentication)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# 📧 電子郵件服務設定 (Email Service)
# 使用 Gmail SMTP 服務
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
FROM_EMAIL="your-email@gmail.com"
FROM_NAME="CRYO-預約系統"

# 👨‍💼 管理員設定 (Admin Configuration)
ADMIN_EMAIL="admin@yourdomain.com"
DEFAULT_ADMIN_PASSWORD="admin123"

# 🔧 可選設定 (Optional Settings)

# Google OAuth (如果需要 Google 登入)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# 應用程式設定
NODE_ENV="development"
APP_NAME="冷凍乾燥設備預約系統"

# 預約限制
MAX_BOOKING_DAYS_AHEAD="30"
MIN_BOOKING_HOURS_AHEAD="2"

# 檔案上傳
MAX_FILE_SIZE="5MB"

# 🚀 部署設定 (Deployment)
# 生產環境請設定為實際域名
# NEXTAUTH_URL="https://your-domain.com"
# VERCEL_URL="your-app.vercel.app"

# ===========================================
# 📝 設定說明:
# 1. 複製此檔案為 .env
# 2. 替換所有 "your-xxx" 為實際值
# 3. DATABASE_URL 是必需的 (PostgreSQL)
# 4. NEXTAUTH_SECRET 請使用隨機字串
# 5. 電子郵件設定用於發送預約通知
# ===========================================
